@model FP.Cloud.OnlineRateTable.Web.Models.ViewModels.ProductCalculationViewModel

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Product Calculation</title>
</head>
<body>
    <h3>Product Calculation</h3>
    <hr />
    @Html.ValidationMessage(FP.Cloud.OnlineRateTable.Web.Controllers.ProductCalculationController.GENERAL_ERROR)
    @{
        var pcalcResult = ViewData.ContainsKey(FP.Cloud.OnlineRateTable.Web.Controllers.ProductCalculationController.PCALC_RESULT) ? 
            ((FP.Cloud.OnlineRateTable.Common.ProductCalculation.PCalcResultInfo)ViewData[FP.Cloud.OnlineRateTable.Web.Controllers.ProductCalculationController.PCALC_RESULT]) : null;

        ViewDataDictionary weightDict = new ViewDataDictionary { { "Weight", pcalcResult != null ? pcalcResult.ProductDescription.Weight : null } };
        ViewDataDictionary postageDict = new ViewDataDictionary { { "Postage", pcalcResult != null ? pcalcResult.ProductDescription.Postage : null } };
        ViewDataDictionary dict = new ViewDataDictionary { { "Pcalc", pcalcResult != null ? pcalcResult.DedicatedDescription : null } };
        var finishButtonStyle = pcalcResult == null || pcalcResult.ProductDescription.State !=
            FP.Cloud.OnlineRateTable.Common.ProductCalculation.EProductDescriptionState.Complete ? "disabled" : string.Empty;
        <div class="container">
            <div class="row">
                <div class="col-md-4">
                    <a class="img-thumbnail"><img src="@Url.Content("~/Content/images/fp_logo.png")" alt="logo" /></a>
                </div>
                <div class="col-md-4">
                    @Html.Partial("~/Views/ProductCalculation/_PostagePartial.cshtml", postageDict)
                </div>
                <div class="col-md-4">
                    @Html.Partial("~/Views/ProductCalculation/_WeightPartial.cshtml", weightDict)
                </div>
            </div>
            <div class="row">
                <div class="col-md-3">

                </div>
                <div class="col-md-9">
                    @if (null == pcalcResult)
                    {
                        @Html.Partial("~/Views/ProductCalculation/_StartPartial.cshtml", ViewData)
                    }
                    else
                    {
                        @Html.Partial(Model.PartialView, Model, dict)
                    }
                </div>
            </div>
            @if (null != pcalcResult)
            {
                <div class="row">
                    <div class="col-md-6">

                    </div>
                    <div class="col-md-2">
                        @Html.ActionLink("Step Back", "StepBack", null, new { @class = "btn btn-primary btn-md btn-block" })
                    </div>
                    <div class="col-md-2">
                        @Html.ActionLink("Restart", "Restart", null, new { @class = "btn btn-primary btn-md btn-block" })
                    </div>
                    <div class="col-md-2">
                        @Html.ActionLink("Finish", "Finish", null, new { @class = "btn btn-primary btn-md btn-block " + finishButtonStyle })
                    </div>
                </div>
            }
        </div>
    }
</body>
</html>

@section scripts
{
    <script type="text/javascript">
        function FormatPostageInput(currentString, formatString, revertOnError)
        {
            $.ajax(
            {
                url: "/ProductCalculation/FormatInput",
                type: "POST",
                data: JSON.stringify(
                {
                    'formatString': formatString,
                    'inputString': currentString,
                }),
                dataType: "json",
                traditional: true,
                contentType: "application/json; charset=utf-8",
                success: function (data) {
                    if (data.Success == true)
                    {
                        $("#EnteredPostage").val(data.DisplayString);
                        $("#EnteredPostageRaw").val(currentString);
                    }
                    else
                    {
                        //Revert to default
                        if (revertOnError == true)
                        {
                            FormatPostageInput("", formatString, false);
                        }
                        else
                        {
                            $("#EnteredPostage").val("");
                            $("#EnteredPostageRaw").val("");
                        }
                    }
                },
                error: function () {
                    $("#EnteredPostage").val("");
                    $("#EnteredPostageRaw").val("");
                }
            });
        }

        $('.number-pad').click(function ()
        {
            var buttonValue = $(this).val();
            var currentString = $("#EnteredPostageRaw").val();
            var formatString = $("#FormatString").val();
            if (buttonValue == "CE")
            {
                if (currentString.length > 0)
                {
                    currentString = currentString.substr(0, currentString.length - 1)
                }
            }
            else
            {
                currentString = currentString + buttonValue;
            }

            FormatPostageInput(currentString, formatString, true);
        });

        $(document).ready(function ()
        {
            var formatString = $("#FormatString").val();
            if (null != formatString)
            {
                FormatPostageInput("", formatString, true);
            }
        });
    </script>
}