Note:
we are not using bower to install bootstrap because this will install a bunch
of .less files that need to be compiled first - thus we would also need grunt
and possibly other stuff.
So these files have been generated using the bootstrap online customizer:
http://getbootstrap.com/customize/